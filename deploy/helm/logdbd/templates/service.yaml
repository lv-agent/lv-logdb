apiVersion: v1
kind: Service
metadata:
  name: {{ include "logdbd.fullname" . }}
  labels:
    {{- include "logdbd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: grpc
      port: {{ .Values.service.port }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "logdbd.selectorLabels" . | nindent 4 }}
